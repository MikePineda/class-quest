# There is no root-level .env. Each app reads its own:
#   backend/.env   <- copy from backend/.env.example  (API settings, secrets)
#   frontend/.env  <- copy from frontend/.env.example (VITE_API_URL)
# In production both are set as Environment variables on the Dokploy apps.
